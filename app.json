{
  "environments": {
    "test": {
      "addons": [
        "heroku-postgresql:hobby-dev"
      ],
      "env": {
        "ON_HEROKU": "True"
      },
      "scripts": {
        "test-setup": "pip install -r development.txt",
        "test" : [
          "pep8 --ignore E501,E712 CTFd/ tests/",
          "nosetests -d"
        ]
      }
    }
  }
}