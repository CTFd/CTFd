{
  "environments": {
    "test": {
      "addons": [
        "heroku-postgresql:hobby-dev"
      ],
      "env": {
        "ON_HEROKU": "True"
      },
      "scripts": {
        "test-setup": "pip install nose"
        "test" : "nosetests -d"
      }
    }
  }
}