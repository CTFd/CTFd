version: "1.0"
services:
  pwn-a:
    mem_limit: 512m
    cpus: 0.25
    build:
      context: .
      args: 
        FLAG: "prac-sec{w0w-U-F0unD-m3}"
        USER: "basic"
    ports:
      - "5001:9001"
    read_only: true
